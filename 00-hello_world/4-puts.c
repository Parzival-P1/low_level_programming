#include <stdio.h>
/**
 * main - prints "Programming is like building a m a multilingual puzz    le"
 * Return: 0 if exited properly, non-zero otherwise
 */
int main ()
{
	puts("Programming is like building a multilingual puzzle\n");
	return (0);
}

