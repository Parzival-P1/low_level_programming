#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first variable to be added
 * @b: second variable to be added
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
